{% extends 'base.html.twig' %}

{% block title %}Benutzerverwaltung – Admin{% endblock %}

{% block body %}
<div class="flex items-center justify-between mb-6">
    <h1 class="text-2xl font-bold text-gray-900">Benutzerverwaltung</h1>
    <a href="{{ path('app_admin') }}" class="btn-secondary text-sm">← Dashboard</a>
</div>

<div class="bg-white shadow rounded-lg overflow-hidden">
    <table class="min-w-full divide-y divide-gray-200">
        <thead class="bg-gray-50">
            <tr>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Benutzername</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Verifiziert</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Admin</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Registriert</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Aktionen</th>
            </tr>
        </thead>
        <tbody class="bg-white divide-y divide-gray-200">
            {% for user in benutzer %}
            <tr class="hover:bg-gray-50">
                <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">
                    {{ user.username }}
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm">
                    {% if user.verified %}
                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">Ja</span>
                    {% else %}
                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-yellow-100 text-yellow-800">Nein</span>
                    {% endif %}
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm">
                    {% if 'ROLE_ADMIN' in user.roles %}
                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-purple-100 text-purple-800">Admin</span>
                    {% else %}
                        <span class="text-gray-400">–</span>
                    {% endif %}
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                    {{ user.erstelltAm|date('d.m.Y H:i') }}
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm">
                    {% if user != app.user %}
                        <form method="post" action="{{ path('app_admin_benutzer_admin_toggle', {id: user.id}) }}" class="inline">
                            <input type="hidden" name="_token" value="{{ csrf_token('admin_toggle_' ~ user.id) }}">
                            <button type="submit" class="text-tisch-700 hover:text-tisch-900 text-xs font-medium">
                                {% if 'ROLE_ADMIN' in user.roles %}Admin entziehen{% else %}Zum Admin machen{% endif %}
                            </button>
                        </form>
                    {% else %}
                        <span class="text-gray-300 text-xs">(du)</span>
                    {% endif %}
                </td>
            </tr>
            {% else %}
            <tr>
                <td colspan="5" class="px-6 py-8 text-center text-sm text-gray-400">Keine Benutzer vorhanden.</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
